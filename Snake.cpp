#include "Snake.hpp"

void Snake::draw_snake(WINDOW *scr){
    // Starting from the current tail, work through and draw each node based on its x, y coords
}

void Snake::add_node(){
    // Add a node
    // set m_current_tail to the new node
}